import 'package:flutter/widgets.dart';

class PersoInventaire extends StatelessWidget{

  const PersoInventaire({super.key});


  @override
  Widget build(BuildContext context) {
    return Text("inventaire");
  }
}